<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="my_mall.mapper.CarouselMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO my_mall.carousel (url, redirect_url, `rank`, create_time, create_user, update_time, update_user)
        VALUES (#{url}, #{redirectUrl}, #{rank}, #{createTime}, #{createUser}, #{updateTime}, #{updateUser})
    </insert>
    <update id="update">
        UPDATE my_mall.carousel
        <set>
            <if test="url != null and url != ''">url = #{url},</if>
            <if test="redirectUrl != null and redirectUrl != ''">redirect_url = #{redirectUrl},</if>
            <if test="rank != null">rank = #{rank},</if>
            <if test="isDeleted != null">is_deleted = #{isDeleted},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="updateUser != null">update_user = #{updateUser},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteBatch">
        delete from my_mall.carousel
        where id in
        <foreach collection="ids" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </delete>

    <select id="getPage" resultType="my_mall.entity.po.Carousel">
        select * from my_mall.carousel
    </select>
    <select id="getById" resultType="my_mall.entity.po.Carousel">
        select * from my_mall.carousel where id=#{id}
    </select>
</mapper>
