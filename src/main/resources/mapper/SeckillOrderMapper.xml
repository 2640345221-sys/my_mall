<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="my_mall.mapper.SeckillOrderMapper">

    <resultMap id="BaseResultMap" type="my_mall.entity.po.SeckillOrder">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="goods_id" property="goodsId"/>
        <result column="order_id" property="orderId"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="getById" resultMap="BaseResultMap">
        SELECT * FROM my_mall.seckill_order WHERE id = #{id}
    </select>

    <select id="pageForAdmin" resultMap="BaseResultMap">
        SELECT * FROM my_mall.seckill_order ORDER BY create_time DESC
    </select>

    <select id="pageForUser" resultMap="BaseResultMap">
        SELECT * FROM my_mall.seckill_order
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
    </select>
    <select id="getByUserIdAndGoodsId" resultMap="BaseResultMap">
        SELECT * FROM my_mall.seckill_order
        WHERE user_id = #{userId} AND goods_id = #{goodsId}
        ORDER BY create_time DESC LIMIT 1
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO my_mall.seckill_order (
            user_id, goods_id, order_id, status, create_time, update_time
        ) VALUES (
                     #{userId}, #{goodsId}, #{orderId}, #{status}, #{createTime}, #{updateTime}
                 )
    </insert>

</mapper>